**/.DS_Store
.vscode/
.venv/
.micropico
__pycache__/
.env
sim/out/
build/
# The firmware writes this beside config.json once the wifi has worked, and
# the simulator writes it here.
wifi_ok

# Firmware images, wherever they end up.
*.uf2

# Left over from act, which this project no longer uses. Ignored rather than
# removed, so an old checkout does not keep offering them up to be committed.
bin/
artifacts/
